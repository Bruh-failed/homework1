
What is the difference between a merge and a rebase?
The difference between merge and rebase is that merging in git is a way to collect the forks that were made and then combine them together while rebase is to update the start of your branch for a commit later to do only after you branch out.

